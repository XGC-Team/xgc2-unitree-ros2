name: ROS 2 Foxy is not in the XGC2 image matrix

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Foxy is not a supported XGC2 build image; use humble or jazzy."
